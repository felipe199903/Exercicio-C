#include <stdio.h>

int main(){
   int valor[5];
   int i;
   for(i=0 ; i < 6 ; i++){
   	
   	  printf("Digite um valor: ");
   scanf("%d", &valor[0]);
   	
   }
 
   for(i=0 ; i < 5 ; i++) {
   	printf("Posicao %d: %d\n", i, valor[i]);
   }
	
}
